# Local Environment

Create a local `.env` file with provider credentials and local gateway settings. Do not commit secret values.

Required values:

- OpenAI provider credential
- Local gateway bearer token
- SQLite database URL
